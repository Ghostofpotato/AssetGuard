- name: REMOTED_ENDPOINT
  description: remoted statistics endpoint
  configuration_parameters:
    ENDPOINT: assetguard-manager-remoted
  metadata:
    endpoint: assetguard-manager-remoted

- name: ANALYSISD_ENDPOINT
  description: analysisd statistics endpoint
  configuration_parameters:
    ENDPOINT: assetguard-manager-analysisd
  metadata:
    endpoint: assetguard-manager-analysisd

- name: ASSETGUARD_DB_ENDPOINT
  description: assetguard-manager-db statistics endpoint
  configuration_parameters:
    ENDPOINT: assetguard-manager-db
  metadata:
    endpoint: assetguard-manager-db
