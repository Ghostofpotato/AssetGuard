name: 'Test uninstall AssetGuard agent package'
description: 'Test uninstall AssetGuard agent package'

inputs:
  tested_docker:
    description: 'image:tag of tested Docker'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Run test uninstall AssetGuard agent package
      shell: bash
      run: |
        sudo docker run \
          -v ${{ github.action_path }}/:/tests \
          -v /tmp/:/packages \
          --entrypoint '/bin/bash' ${{ inputs.tested_docker }} \
          -c "/tests/smoke_uninstall_test.sh" 
